# 📱 플래너 앱 상세기획서 (가칭: **pyosition**)

## 1. 앱 컨셉

* **슬로건**: “삶을 통제 가능한 시스템으로 설계한다.”
* **핵심 가치**:

  1. 매일 `S`(Status) 기록 → 현실 데이터 축적
  2. 매주 `I`(Importance) 배정 → 방향성 결정
  3. 기록은 자책이 아니라 “버전 로그”로 관리

---

## 2. 사용자 시나리오

1. **아침/낮**: 앱을 굳이 열 필요 없음. (부담 최소화)
2. **저녁 자기 전**: 5분 → 각 모듈별 `S` 상태 체크 (−2 \~ +2)
3. **일요일 밤**: 지난 주 `S` 추세 확인 → 다음 주 `I`(0\~3) 가중치 배정
4. **분기별**: 모듈 추가/삭제/합병 여부 점검

---

## 3. 주요 기능

### 3.1 모듈 관리

* 기본 제공 모듈: 건강 / 음식 / 수면 / 돈 / 관계 / 일 / 취미
* 사용자 자유롭게 **추가/삭제/병합** 가능
* 각 모듈마다 “해석 정책” 선택

  * 최적대역형 / 적을수록-좋음 / 많을수록-좋음

### 3.2 상태 기록 (Daily S)

* 매일 각 모듈을 \*\*슬라이더(−2 \~ +2)\*\*로 기록
* 기록 시 간단 메모 가능 (“야식 먹음”, “불광천 못 감”)
* `S=±2`인 경우 자동 하이라이트 → “응급 보정 팁” 표시

### 3.3 중요도 배정 (Weekly I)

* 주간 리포트 화면 → 지난 7일 `S` 그래프와 평균
* 자동 추천: 부족 지속 → `I↑`, 과잉 지속 → `I↓`
* 사용자가 최종 결정 (슬라이더 0\~3)
* `I=3`은 최대 2개까지만 선택 가능

### 3.4 분기 리밸런싱

* 분기별 리뷰 모드 →

  * “3주 연속 I=3” 모듈 있으면 **환경 레벨 개선 필요** 알림
  * 모듈 추가/삭제/병합 UI 제공

### 3.5 버전 로그

* 매일 기록과 주간 결정이 쌓이면 자동으로 **패치노트 형식**으로 변환
* 예:

  ```
  pyopatch v25.08.20
  - [S] 음식 +2 (폭식), 수면 -1 (늦게 잠듦)
  - [I] 건강=3, 돈=2, 관계=1
  - 개선: 저녁 폭식 방지, 불광천 fallback 추가
  ```

### 3.6 알림/리마인드

* 매일 밤 11시 → “S 기록 알림”
* 일요일 밤 10시 → “I 배정 알림”
* `S=±2` 기록 시 → 즉시 보정 알림 (“지금 한 번 산책해보면 어때요?”)

---

## 4. 정보 구조 (IA)

* **홈**: 오늘 S 기록 요약 + 이번주 I 상태
* **기록**:

  * Daily S (리스트뷰 + 슬라이더)
  * Weekly I (그래프 + 슬라이더)
  * History (버전로그 형식)
* **모듈 관리**: 모듈 추가/삭제/병합, 해석 정책 선택
* **리포트**: 주간/분기 리포트

---

## 5. UX 원칙

1. **기록은 2분 이내** → 복잡하면 지속 안 됨
2. **자책 금지, 로그만 남기기** → 개선은 “다음 버전”에서
3. **시각화는 간단**: 모듈별 막대그래프, 추세 라인, 버전로그
4. **I=3 강조**: 이번 주에 집중할 모듈만 홈에서 두드러지게
